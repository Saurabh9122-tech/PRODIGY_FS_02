/* Reset base styling */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: #e6f4ff; /* Soft sky blue background */
  color: #333;
  line-height: 1.6;
  padding: 30px;
}

/* Heading */
h1 {
  text-align: center;
  font-size: 28px;
  margin-bottom: 40px;
  color: #007acc;
}

/* Form container */
.form-container {
  background: #ffffff;
  max-width: 480px;
  margin: auto;
  padding: 30px 25px;
  border-radius: 12px;
  box-shadow: 0 4px 20px rgba(0, 122, 204, 0.1);
  transition: box-shadow 0.3s ease-in-out;
}

.form-container:hover {
  box-shadow: 0 6px 25px rgba(0, 122, 204, 0.2);
}

input[type="text"],
input[type="email"],
input[type="password"] {
  width: 100%;
  padding: 12px;
  margin-bottom: 20px;
  border: 1px solid #cce7ff;
  border-radius: 8px;
  font-size: 15px;
  transition: border-color 0.2s;
}

input:focus {
  border-color: #007acc;
  outline: none;
}

/* Buttons */
button {
  background-color: #00aaff;
  color: white;
  padding: 12px;
  width: 100%;
  border: none;
  border-radius: 8px;
  font-weight: bold;
  font-size: 15px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

button:hover {
  background-color: #008ecc;
}

/* Table */
table {
  width: 100%;
  margin-top: 50px;
  border-collapse: collapse;
  background-color: white;
  box-shadow: 0 4px 15px rgba(0, 122, 204, 0.1);
  border-radius: 10px;
  overflow: hidden;
}

th,
td {
  text-align: left;
  padding: 16px;
  border-bottom: 1px solid #f0f0f0;
}

th {
  background-color: #00aaff;
  color: white;
  font-weight: 600;
  font-size: 15px;
}

tr:hover {
  background-color: #f0faff;
}

/* Inline edit/delete buttons */
td button {
  padding: 8px 12px;
  border: none;
  border-radius: 6px;
  font-size: 13px;
  margin-right: 5px;
  cursor: pointer;
  transition: background-color 0.2s;
}

/* Edit */
td button.edit {
  background-color: #007acc;
  color: white;
}

td button.edit:hover {
  background-color: #005f99;
}

/* Delete */
td button.delete {
  background-color: #ff4d4d;
  color: white;
}

td button.delete:hover {
  background-color: #cc0000;
}
